using Metapsi;

namespace MdsCommon
{
    //public static class DataModel
    //{
    //    public static Relation.Children<IServiceSnapshotData, ServiceConfigurationSnapshot> ServiceSnapshot =
    //        Relation.On<IServiceSnapshotData, ServiceConfigurationSnapshot>(snaphot =>
    //        {
    //            snaphot.FromParentId(x => x.Id, snaphot =>
    //            {
    //                snaphot.Children(x => x.ServiceConfigurationSnapshotParameters, x => x.ServiceConfigurationSnapshotId, s => { });
    //            });
    //        });
    //}
}
